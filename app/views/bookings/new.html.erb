<h1>Make a New Booking!</h1>

<%= form_for(@booking) do |f| %>
    <%= f.label :date %>
    <%= f.text_field :date %>

    <%= f.hidden_field :user_id, value: session[:user_id] %>

    <%= f.submit %> 

<% end %>