<h1>All Bookings</h1>

<% @bookings.each do |booking| %>
    <p>You have a booking with: <%= booking.barber.name %></p><br>
    <img src="<%= booking.barber.image_url %>" alt="barber avatar" width="250" height="250">    
    <p>On: <%= booking.date %></p>
<% end %>